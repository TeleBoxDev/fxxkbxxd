# fxxkbxxd

pnpm + monorepo + typescript

## Usage

```shell
git clone 'https://.....'
cd fxxkbxxd

# 安装 pnpm（如果尚未安装）
npm install -g pnpm

# 安装依赖
pnpm install

# 配置环境变量
cp apps/client/.env.example apps/client/.env
vim apps/client/.env

# 启动项目
pnpm start
```
